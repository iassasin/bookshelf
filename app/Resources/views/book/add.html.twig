{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Add book</h1>

        <div class="row block book">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
